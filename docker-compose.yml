services:
  app:
    image: 'jc21/nginx-proxy-manager:latest'
    restart: unless-stopped
    ports:
      - ${HTTP_PORT}:80
      - ${MANAGER_PORT}:81
      - ${HTTPS_PORT}:443
    volumes:
      - ./data:/data
      - ./letsencrypt:/etc/letsencrypt